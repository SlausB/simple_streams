import Space from '../../lib/space'

const space = new Space
space.s( 'booleans_array', [ true, false, true ] )
space.s( 'strings_array', [ 'first', 'second', 'third' ] )
space.s( 'numbers_array', [ 101, 102, 103 ] )
